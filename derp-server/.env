# =============================
# DERP 服务器配置（全注释永久记忆版）
# 每台服务器只改下面这两行就行：
# =============================

# 【必改】本机对外的主机名（必须和证书、DNS、derp-map.yaml 里保持一致）
DERP_HOSTNAME=derp-aly.grep2000.com

# 【必改】只允许这个 Headscale 签发的客户端使用（关门谢客模式）
DERP_VERIFY_CLIENTS=headscale.grep2000.com

# =============================
# 下面这些默认就够用了，但写出来是为了让你永远记得它们可以改
# =============================

# 证书模式：manual = 手动放证书，letsencrypt = 自动申请（我们不用）
DERP_CERT_MODE=manual

# 证书存放目录（容器内路径，和 volumes 挂载要对应）
DERP_CERT_DIR=/app/certs

# DERP HTTPS 监听地址（:8443 是标准端口，基本不动）
DERP_ADDR=:8443

# 是否开启 STUN 服务（必须开，否则打洞失败率暴涨）
DERP_STUN=true

# STUN UDP 端口（全世界标准就是 3478，永远别改）
DERP_STUN_PORT=3478

# =============================
# 可选增强项（需要时再取消注释）
# =============================

# 如果你想多个 DERP 节点之间互相 mesh（推荐开），在这里写同一个超长密码
# DERP_MESH_PASSWORD=your-super-secret-mesh-password-2025

# 如果你想看更详细日志（调试时打开）
# DERP_DEBUG=true
